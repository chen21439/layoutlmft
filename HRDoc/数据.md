 | 阶段   | 输出字段                                                      | 说明                                      |
  |--------|---------------------------------------------------------------|-------------------------------------------|
  | stage1 | text, box, page                                               | PDF解析，提取文本行                       |
  | stage2 | text, box, page                                               | 检测table/figure/equation并合并，字段不变 |
  | stage3 | text, box, page, class, is_meta, line_id, parent_id, relation | 模型推理，添加所有额外字段                |
| class     | ly_pred 模型预测              | Stage 3 推理           |
  | is_meta   | 根据 class 推断               | Stage 3 推理           |
  | line_id   | 循环索引 cl_id                | Stage 3 推理时直接赋值 |
  | parent_id | pa_pred 模型预测              | Stage 3 推理           |
  | relation  | re_pred 模型预测              | Stage 3 推理           |



  stage3 添加的字段（recover_json函数，第78-87行）

  valid_json.append({
      "text": texts_list[cl_id],
      "box": bboxes_list[cl_id],
      "class": cls_,                    # 类别预测 (title, section, etc.)
      "page": id2page[cl_id],
      "is_meta": True if cls_ not in cfg.ly_vocab.struct_words else False,  # 是否元信息
      "line_id": cl_id,                 # 行ID
      "parent_id": -1 if pa_pred_list[cl_id] == 0 else pa_pred_list[cl_id], # 父节点ID
      "relation": "meta" if not ly_pred_mask_list[cl_id] else cfg.re_vocab._ids_words_map[re_pred_list[cl_id]]  # 关系类型
  })

