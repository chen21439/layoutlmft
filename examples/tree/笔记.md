
# 阶段2

## 本地测试

```bash
# 快速测试
  python examples/train_parent_finder.py --mode simple  --max_chunks 1  --num_epochs 3
  
      
# 完整训练
python examples/train_parent_finder.py --mode full --batch_size 2 --num_epochs 10
      
```

## 云服务器

```bash

  python examples/train_parent_finder.py \
      --mode simple \
      --features_dir /home/linux/code/data/line_features \
      --output_dir /home/linux/code/layoutlmft/outputs \
      --max_chunks 1 \
      --num_epochs 3


  python examples/train_parent_finder.py \
      --mode full \
      --features_dir /home/linux/code/data/line_features \
      --output_dir /home/linux/code/layoutlmft/outputs \
      --batch_size 4 \
      --learning_rate 2e-4 \
      --num_epochs 15 \
      --use_soft_mask
```

# 阶段4

```bash
 python ./examples/tree/inference_build_tree.py --max_samples 1 --save_json --save_markdown --save_ascii

```