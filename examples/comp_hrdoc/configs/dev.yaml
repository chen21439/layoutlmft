# Dev 环境配置 (本地开发)

# 数据路径
data:
  root: "/mnt/e/models/data/Section/Comp_HRDoc"
  train_dir: "/mnt/e/models/data/Section/Comp_HRDoc/HRDH_MSRA_POD_TRAIN"
  test_dir: "/mnt/e/models/data/Section/Comp_HRDoc/HRDH_MSRA_POD_TEST"
  features_dir: "/mnt/e/models/data/Section/Comp_HRDoc/region_features"

# Artifact 路径（实验管理）
artifact:
  root: "/mnt/e/models/data/layoutlmft/artifact_comp_hrdoc"

# GPU 配置
gpu:
  cuda_visible_devices: null

# 输出路径
output:
  root: "outputs"

# 推理配置 (FastAPI 服务使用)
inference:
  checkpoint_path: null
  data_dir_base: "/mnt/e/programFile/AIProgram/tender_ontology/static/upload"
